import { ProfileScreenNavigationProp } from '../../../screens/main';
import { ProfileProps } from '../../../shared/types';

type ProfileCardProps = {
  navigation: ProfileScreenNavigationProp;
  data: ProfileProps;
};

export default ProfileCardProps;
